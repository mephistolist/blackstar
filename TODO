Need configure file to execute without having to chmod +x it after download. 
The man page should be installed without the user having to copy the man file.
Maybe work to allow single usage mode with keys instead of only passwords. 
Or maybe just advise key use for hosts file.
